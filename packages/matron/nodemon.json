{
  "watch": ["src/**/*.ts"],
  "execMap": {
    "ts": "ts-node --compilerOptions '{\"module\":\"commonjs\"}'",
    "js": "node"
  },
  "events": {
    "start": "npm run build",
    "restart": "npm run build"
  }
}
