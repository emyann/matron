{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "prestart": "npm run bootstrap",
    "start": "npx lerna exec --scope @matron/schematics -- npm run start",
    "build": "npx lerna exec --scope @matron/schematics --scope matron -- npm run build",
    "publish": "npm run build && npx lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.6.0"
  }
}
