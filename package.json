{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna add --exact @matron/schematics --scope=matron & lerna add --exact --dev @matron/templates --scope=@matron/schematics & lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.6.0"
  }
}
